openapi: 3.1.0
info:
  title: Zeam Platform API Gateway
  description: |
    Placeholder. The authoritative spec lives in `zeam-api-gateway.go` at
    `docs/openapi.yaml` and is synced into this repo via `npm run
    sync-spec`. Do not hand-edit this file — regenerate it from the
    gateway tag you are targeting.
  version: 0.0.0-unreleased
servers:
  - url: https://api-gateway.zeam.app
    description: Production
paths: {}
components: {}
